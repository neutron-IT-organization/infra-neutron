apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: sonarqube
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: sonarqube
    weight: 100
  port:
    targetPort: 9000
  wildcardPolicy: None